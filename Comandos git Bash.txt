cd - carrega diretório
ls - lista conteúdo do diretório
git clone (endereço do repositório)
git checkout -b william

git status
git add (nome do arquivo 1) (nome do arquivo 2)
git commit -m "Atualização da classe Pessoa"
git push origin william

/* comentários são feitos assim */
git pull origin william

/* comando pwd para listar o diretório*/
